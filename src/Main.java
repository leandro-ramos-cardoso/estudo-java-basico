public class Main {
    public static void main(String[] args) {
        // Sintaxe <tipo> <nome> = <valor inicial> (opcional)
        int idade = 25;
        double altura = 1.68;
        char sexo = 'M';

        // Tipo primitivos em Java

        // TIPO (byte)
        // TAMANHO -> 8 bits (2 x 2 x 2 x 2 x 2 x 2 x 2 x 2) = 2^8 = 256 possibilidades.
        // VALORES -> -128 a 127
        // VALOR PADRÃO -> 0

        // TIPO (short)
        // TAMANHO -> 16 bits
        // VALORES -> -32768 a 32767
        // VALOR PADRÃO -> 0

        // TIPO (int)
        // TAMANHO -> 32 bits
        // VALORES -> -2147483648 a 2147483647
        // VALOR PADRÃO -> 0

        // TIPO (long)
        // TAMANHO -> 64 bits
        // VALORES -> -9223372036854770000 a 9223372036854770000
        // VALOR PADRÃO -> 0L

        // TIPO (float)
        // TAMANHO -> 32 bits
        // VALORES -> -1,4024E-37 a 3,4028E+38
        // VALOR PADRÃO -> 0.0f

        // TIPO (double)
        // TAMANHO -> 64 bits
        // VALORES -> -4,94E-307 a 1,79E+308
        // VALOR PADRÃO -> 0.0

        // TIPO (char)
        // TAMANHO -> 16 bits
        // VALORES -> '\u0000' a '\uFFFF'
        // VALOR PADRÃO -> '\u0000'

        // TIPO (boolean)
        // TAMANHO -> 1 bit
        // VALORES -> {false, true}
        // VALOR PADRÃO -> false

        // Bits - quantidade de valores possiveis diferentes que uma váriavel pode armazenar.

        // String - cadeia de caracteres (palavras ou texto)

        // Cada bit = 2 possibilidades

        // COMO DECLARAR OS NOMES DAS VARIÁVEIS

        // USAR O MODELO: camelCase

        // 3 Operações básicas
        // Entrada de dados - Usuário insere dados no PC atraves de dispositivos de entrada (LEITURA)

        // Processamento de dados - Programa realiza cálculos (ATRIBUIÇÃO)

        // Saida de dados - Programa informa dados para o usuário, usando dispositivos de saida de dados como o monitor (ESCRITA)
    }
}